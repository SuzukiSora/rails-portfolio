<header class="fixed-top">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <% if user_signed_in? %>
      <a class="navbar-brand" href="/"><%= current_user.name %></a>
    <% else %>
      <a class="navbar-brand" href="/">Movie</a>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <% if user_signed_in? %>
        <ul class="navbar-nav login_nav">
          <li class="nav-item"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></li>
          <li class="nav-item"><%= link_to 'ユーザ一検索', users_path %></li>
          <li class="nav-item"><%= link_to '投稿検索', posts_path %></li>
        </ul>
      <% else %>
        <ul class="navbar-nav">
          <li class="nav-item"><%= link_to '新規登録', new_user_registration_path %></li>
          <li class="nav-item"><%= link_to 'ログイン', new_user_session_path %></li>
        </ul>
      <% end %>
    </div>
  </nav>
</header>
